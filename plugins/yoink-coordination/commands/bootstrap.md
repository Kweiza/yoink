---
description: Create yoink opt-in labels in this repo (run once per repo)
---

Please run this bash command and show its output verbatim, with no additional commentary:

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/bin/yoink-bootstrap"
```
